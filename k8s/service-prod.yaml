apiVersion: v1
kind: Service
metadata:
  name: indibank-core
  namespace: indibank
  labels:
    app: indibank-core
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: indibank-core
    slot: green
